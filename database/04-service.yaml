apiVersion: v1
kind: Service
metadata:
  name: postgres-svc
  namespace: database # Isolado na camada de dados
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
  selector:
    app: postgres